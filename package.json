{
  "name": "xurface-discern",
  "version": "0.1.0",
  "private": true,
  "description": "Xurface Discern - approve, edit or deny the critical actions AI agents take on your behalf. One web app, shipped native to Android, Apple and Windows.",
  "scripts": {
    "test": "node --test --test-timeout=20000 test/",
    "serve": "npx --yes http-server www -p 5173 -c-1 -o",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios",
    "cap:sync": "cap sync",
    "android:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:aab": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
